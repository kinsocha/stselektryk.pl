---
---

<style>
  header {
    position: relative;
    background-color: white;
    padding: 4rem 0;
    overflow: hidden;
  }

  @media (min-width: 1024px) {
    header {
      padding: 6rem 0;
    }
  }

  .hero-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1rem;
    position: relative;
    z-index: 10;
  }

  .hero-content {
    max-width: 66%;
  }

  @media (max-width: 1023px) {
    .hero-content {
      max-width: 100%;
    }
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--color-gray-900);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  @media (min-width: 1024px) {
    h1 {
      font-size: 3.75rem;
    }
  }

  h1 span {
    color: var(--color-primary);
  }

  .hero-text {
    font-size: 1.25rem;
    color: var(--color-gray-600);
    margin-bottom: 2.5rem;
    max-width: 42rem;
  }

  .hero-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  @media (min-width: 640px) {
    .hero-buttons {
      flex-direction: row;
    }
  }

  .hero-bg {
    position: absolute;
    top: 0;
    right: 0;
    width: 33%;
    height: 100%;
    background-color: var(--color-blue-50);
    transform: skewX(12deg) translateX(5rem);
    display: none;
  }

  @media (min-width: 1024px) {
    .hero-bg {
      display: block;
    }
  }
</style>

<header>
  <div class="hero-container">
    <div class="hero-content">
      <h1>
        Super temperatura w Twoim domu <span>przez cały rok.</span>
      </h1>
      <p class="hero-text">
        Zajmujemy się profesjonalnym doborem, montażem i serwisem klimatyzacji. Postaw na sprawdzone rozwiązania i ciesz się czystym powietrzem.
      </p>
      <div class="hero-buttons">
        <a href="#wybor" class="btn btn-primary">Wybierz Klimatyzator</a>
        <a href="tel:+48123456789" class="btn btn-outline">Zadzwoń: +48 123 456 789</a>
      </div>
    </div>
  </div>
  <div class="hero-bg"></div>
</header>